# DE: Bundle-Routes für Showcase-Controller
# EN: Bundle routes for showcase controller
#
# DE: In der Host-App einbinden via config/routes/tabler.yaml:
# EN: Import in host app via config/routes/tabler.yaml:
#
#   tabler_bundle:
#       resource: '@TablerBundle/config/routes.yaml'

tabler_showcase:
    resource: '../src/Controller/'
    type: attribute
